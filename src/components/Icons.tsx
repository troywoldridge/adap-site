// src/components/Icons.tsx

// import the raw SVG React components
import {
  PiggyBank,
  Award,
  Clock,
  Check,
} from "lucide-react";

// re-export them under the names your pages expect
export const PiggyBankIcon = PiggyBank;
export const RibbonIcon = Award;       // “Award” is a ribbon/trophy-style icon
export const ClockIcon = Clock;
export const CheckIcon = Check;
