/**
 * NOTE:
 * This project is currently focused on production runtime code.
 * These tests are intentionally disabled to keep `tsc --noEmit` clean.
 *
 * Re-enable later when price module paths/types are finalized.
 */
export {};
