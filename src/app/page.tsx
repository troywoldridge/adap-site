import Hero from '@/components/Hero'

export default function HomePage() {
  return (
    <section>
      <Hero />
      <h1>🎉 Tailwind & App Router are Working!</h1>
      <p>Now let’s build your layout one piece at a time.</p>
    </section>
  )
}
